apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bahmni.fullname" . }}-test-connection"
  labels:
    {{- include "bahmni.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bahmni.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
