'use strict';

angular.module('bahmni.appointments')
    .controller('AppointmentsManageController', ['$scope',
        function ($scope) {
            var init = function () {
            };
            return init();
        }]);
