'use strict';

angular.module('bahmni.appointments')
    .controller('AppointmentsManageController', ['$scope', '$location', '$state', 'uiCalendarConfig',
        function ($scope, $location, $state, uiCalendarConfig) {
            var init = function () {

            };
            return init();
        }]);
