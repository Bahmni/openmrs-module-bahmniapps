'use strict';

angular.module('bahmni.appointments')
    .controller('AppointmentsAdminController', ['$scope',
        function ($scope) {
            var init = function () {
            };
            return init();
        }]);
