'use strict';

angular.module('bahmni.appointments')
    .controller('ManageAppointmentsController', ['$scope',
        function ($scope) {
            var init = function () {
            };
            return init();
        }]);
